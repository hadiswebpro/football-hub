import { formatDate, getLeagueStatus } from "../utils/date";
import { getLeagueFixtures, getLeagueRecentFixtures } from "../api/matches";
import { getLeagueStandings } from "../api/standings";
import { getLeagueTeams } from "../api/teams";
import createMatchCard from "../components/matchcard";

const SELECTED_LEAGUE_KEY = "football-hub-selected-league";
const SELECTED_TEAM_KEY = "football-hub-selected-team";
const FAVORITES_KEY = "football-hub-favorite-leagues";

function getSelectedLeague() { try { return JSON.parse(sessionStorage.getItem(SELECTED_LEAGUE_KEY)); } catch { return null; } }
function getFavorites() { try { return JSON.parse(localStorage.getItem(FAVORITES_KEY)) || []; } catch { return []; } }

function normalizeFixture(item) {
    const fixture = item.fixture ?? {}, league = item.league ?? {}, teams = item.teams ?? {}, goals = item.goals ?? {};
    const status = fixture.status?.short, isLive = ["1H", "HT", "2H", "ET", "BT", "P", "LIVE"].includes(status), isFinished = ["FT", "AET", "PEN"].includes(status), date = fixture.date;
    return { fixtureId: fixture.id, date, league: league.name ?? "League", status: isLive ? "LIVE" : isFinished ? "FINISHED" : "SCHEDULED", minute: fixture.status?.elapsed ? `${fixture.status.elapsed}'` : "", time: date ? new Date(date).toLocaleTimeString("en-GB", { hour: "2-digit", minute: "2-digit" }) : "–", home: { name: teams.home?.name ?? "Home", logo: teams.home?.logo ?? "", score: goals.home }, away: { name: teams.away?.name ?? "Away", logo: teams.away?.logo ?? "", score: goals.away } };
}

function renderFixtureGrid(target, fixtures, emptyMessage) {
    if (!fixtures.length) { target.innerHTML = `<div class="league-page__empty league-page__empty--inline"><span>NO DATA</span><h2>No matches found</h2><p>${emptyMessage}</p></div>`; return; }
    target.innerHTML = "";
    fixtures.forEach((fixture) => target.appendChild(createMatchCard(fixture)));
}

function renderStandings(target, standings) {
    if (!standings.length) { target.innerHTML = `<div class="league-page__empty league-page__empty--inline"><span>NO DATA</span><h2>No standings found</h2><p>There is no standings data available for this season.</p></div>`; return; }
    target.innerHTML = `<div class="league-page__table-wrap"><table class="league-page__table"><thead><tr><th>#</th><th>Team</th><th>MP</th><th>W</th><th>D</th><th>L</th><th>GD</th><th>PTS</th><th>Form</th></tr></thead><tbody>${standings.map((item) => { const team = item.team ?? {}, goals = item.goalsDiff ?? 0, form = (item.form ?? "").slice(-5).split("").map((result) => { const className = result === "W" ? "win" : result === "D" ? "draw" : result === "L" ? "loss" : ""; return `<span class="league-page__form-dot league-page__form-dot--${className}" title="${result}">${result}</span>`; }).join(""); return `<tr><td class="league-page__rank">${item.rank ?? "–"}</td><td><div class="league-page__table-team"><img src="${team.logo ?? ""}" alt="${team.name ?? "Team"} logo"><strong>${team.name ?? "Team"}</strong></div></td><td>${item.all?.played ?? 0}</td><td>${item.all?.win ?? 0}</td><td>${item.all?.draw ?? 0}</td><td>${item.all?.lose ?? 0}</td><td>${goals > 0 ? `+${goals}` : goals}</td><td class="league-page__points">${item.points ?? 0}</td><td><div class="league-page__form">${form || "–"}</div></td></tr>`; }).join("")}</tbody></table></div>`;
}

function renderLeagueTeams(target, teams) {
    if (!teams.length) { target.innerHTML = `<div class="league-page__empty league-page__empty--inline"><span>NO DATA</span><h2>No teams found</h2><p>There is no team data available for this season.</p></div>`; return; }
    target.innerHTML = `<div class="league-page__team-grid">${teams.map(({ team = {} }) => `<button class="league-page__team-card" type="button" data-team-id="${team.id ?? ""}" data-team-name="${team.name ?? "Team"}" data-team-logo="${team.logo ?? ""}" data-team-country="${team.country ?? "International"}"><div class="league-page__team-logo"><img src="${team.logo ?? ""}" alt="${team.name ?? "Team"} logo"></div><div class="league-page__team-info"><h3>${team.name ?? "Team"}</h3><p>${team.country ?? "International"}</p></div><div class="league-page__team-footer"><span>Open team</span><span>↗</span></div></button>`).join("")}</div>`;
}

async function loadLeagueMatches(app, competition) {
    const season = competition.seasons?.find((item) => item.current === true) ?? competition.seasons?.[0], leagueId = competition.league.id, liveTarget = app.querySelector("[data-fixtures='live']"), upcomingTarget = app.querySelector("[data-fixtures='upcoming']"), recentTarget = app.querySelector("[data-fixtures='recent']");
    if (!season?.year) { const message = `<div class="league-page__empty league-page__empty--inline"><span>SEASON UNAVAILABLE</span><h2>Fixtures unavailable</h2><p>This competition does not have a season selected yet.</p></div>`; liveTarget.innerHTML = message; upcomingTarget.innerHTML = message; recentTarget.innerHTML = message; return; }
    liveTarget.innerHTML = `<div class="league-page__loading">Loading live matches…</div>`; upcomingTarget.innerHTML = `<div class="league-page__loading">Loading upcoming matches…</div>`; recentTarget.innerHTML = `<div class="league-page__loading">Loading recent results…</div>`;
    try {
        const [upcomingData, recentData] = await Promise.all([getLeagueFixtures(leagueId, season.year, 12), getLeagueRecentFixtures(leagueId, season.year, 12)]);
        const allUpcoming = (upcomingData.response ?? []).map(normalizeFixture), allRecent = (recentData.response ?? []).map(normalizeFixture);
        const combined = [...allUpcoming, ...allRecent];
        const live = combined.filter((fixture, index, fixtures) => fixture.status === "LIVE" && fixtures.findIndex((item) => item.fixtureId === fixture.fixtureId) === index).sort((a, b) => new Date(a.date) - new Date(b.date));
        const upcoming = allUpcoming.filter((fixture) => fixture.status === "SCHEDULED").sort((a, b) => new Date(a.date) - new Date(b.date));
        const recent = allRecent.filter((fixture) => fixture.status === "FINISHED").sort((a, b) => new Date(b.date) - new Date(a.date));
        renderFixtureGrid(liveTarget, live, "There are no live matches from this competition right now."); renderFixtureGrid(upcomingTarget, upcoming, "There are no upcoming fixtures available for this season."); renderFixtureGrid(recentTarget, recent, "There are no completed results available for this season.");
        app.querySelector("[data-count='live']").textContent = `${live.length} live`; app.querySelector("[data-count='upcoming']").textContent = `${upcoming.length} fixture${upcoming.length === 1 ? "" : "s"}`; app.querySelector("[data-count='recent']").textContent = `${recent.length} result${recent.length === 1 ? "" : "s"}`;
        app.querySelector("[data-overview='upcoming']").textContent = upcoming.length; app.querySelector("[data-overview='recent']").textContent = recent.length;
    } catch { const error = `<div class="league-page__empty league-page__empty--inline"><span>API ERROR</span><h2>Could not load matches</h2><p>Please check your API key and try again.</p></div>`; liveTarget.innerHTML = error; upcomingTarget.innerHTML = error; recentTarget.innerHTML = error; }
}

async function loadLeagueTable(app, competition) {
    const season = competition.seasons?.find((item) => item.current === true) ?? competition.seasons?.[0], target = app.querySelector("[data-standings]");
    if (!target) return;
    if (!season?.year) { target.innerHTML = `<div class="league-page__empty league-page__empty--inline"><span>SEASON UNAVAILABLE</span><h2>Standings unavailable</h2><p>This competition does not have a season selected yet.</p></div>`; return; }
    target.innerHTML = `<div class="league-page__loading">Loading league table…</div>`;
    try { const data = await getLeagueStandings(competition.league.id, season.year), standings = (data.response?.[0]?.league?.standings ?? []).flat(); renderStandings(target, standings); app.querySelector("[data-count='table']").textContent = `${standings.length} team${standings.length === 1 ? "" : "s"}`; const leader = standings[0]?.team?.name ?? "—", leaderPoints = standings[0]?.points ?? "—"; app.querySelector("[data-overview='leader']").textContent = leader; app.querySelector("[data-overview='points']").textContent = leaderPoints; } catch { target.innerHTML = `<div class="league-page__empty league-page__empty--inline"><span>API ERROR</span><h2>Could not load table</h2><p>Please check your API key and try again.</p></div>`; }
}

async function loadLeagueTeams(app, competition) {
    const season = competition.seasons?.find((item) => item.current === true) ?? competition.seasons?.[0], target = app.querySelector("[data-teams]");
    if (!target) return;
    if (!season?.year) { target.innerHTML = `<div class="league-page__empty league-page__empty--inline"><span>SEASON UNAVAILABLE</span><h2>Teams unavailable</h2><p>There is no team data available for this season.</p></div>`; return; }
    target.innerHTML = `<div class="league-page__loading">Loading league teams…</div>`;
    try { const data = await getLeagueTeams(competition.league.id, season.year), teams = data.response ?? []; renderLeagueTeams(target, teams); app.querySelector("[data-count='teams']").textContent = `${teams.length} team${teams.length === 1 ? "" : "s"}`; app.querySelector("[data-overview='teams']").textContent = teams.length; target.querySelectorAll("[data-team-id]").forEach((card) => card.addEventListener("click", () => { sessionStorage.setItem(SELECTED_TEAM_KEY, JSON.stringify({ id: Number(card.dataset.teamId), name: card.dataset.teamName, country: card.dataset.teamCountry, logo: card.dataset.teamLogo, league: competition.league.name })); window.location.hash = "#team"; })); } catch { target.innerHTML = `<div class="league-page__empty league-page__empty--inline"><span>API ERROR</span><h2>Could not load teams</h2><p>Please check your API key and try again.</p></div>`; }
}

function createLeaguePage() {
    const app = document.querySelector("#app"), competition = getSelectedLeague();
    if (!app || !competition?.league) return;
    const currentSeason = competition.seasons?.find((season) => season.current === true) ?? competition.seasons?.[0], status = currentSeason ? getLeagueStatus(currentSeason.start, currentSeason.end) : "Unknown", isFavorite = getFavorites().includes(competition.league.id);
    app.innerHTML = `<section class="league-page"><button class="league-page__back" type="button"><span aria-hidden="true">←</span> Back to leagues</button><header class="league-page__hero"><div class="league-page__hero-glow" aria-hidden="true"></div><div class="league-page__logo"><img src="${competition.league.logo}" alt="${competition.league.name} logo"></div><div class="league-page__identity"><span class="league-page__eyebrow">FOOTBALL COMPETITION</span><h1>${competition.league.name}</h1><p>${competition.country?.name ?? "International"} <span>•</span> ${competition.league.type ?? "League"}</p></div><button class="league-page__favorite ${isFavorite ? "is-favorite" : ""}" type="button" aria-pressed="${isFavorite}"><span aria-hidden="true">★</span><span>Favorite</span></button></header><div class="league-page__meta"><div><span>SEASON</span><strong>${currentSeason?.year ?? "—"}</strong></div><div><span>STATUS</span><strong class="league-page__status league-page__status--${status.toLowerCase()}"><i></i>${status}</strong></div><div><span>START</span><strong>${currentSeason ? formatDate(currentSeason.start) : "—"}</strong></div><div><span>END</span><strong>${currentSeason ? formatDate(currentSeason.end) : "—"}</strong></div></div><nav class="league-page__tabs" role="tablist" aria-label="League details"><button class="league-page__tab is-active" type="button" data-panel="overview">Overview</button><button class="league-page__tab" type="button" data-panel="matches">Matches</button><button class="league-page__tab" type="button" data-panel="table">Table</button><button class="league-page__tab" type="button" data-panel="teams">Teams</button></nav><section class="league-page__content is-active" data-content="overview"><div class="league-page__section-heading"><span>LEAGUE CENTER</span><h2>${competition.league.name}</h2><p>Follow fixtures, results and standings from this competition.</p></div><div class="league-page__overview-stats"><article><span>TEAMS</span><strong data-overview="teams">—</strong></article><article><span>UPCOMING</span><strong data-overview="upcoming">—</strong></article><article><span>RECENT RESULTS</span><strong data-overview="recent">—</strong></article><article><span>LEADER</span><strong data-overview="leader">—</strong></article><article><span>LEADER POINTS</span><strong data-overview="points">—</strong></article></div><div class="league-page__overview-grid"><article class="league-page__panel league-page__panel--featured"><span class="league-page__panel-label">UPCOMING</span><div class="league-page__panel-icon">→</div><h3>Upcoming matches</h3><p>Jump to the next fixtures from this competition.</p><button class="league-page__panel-link" type="button" data-jump="matches">View fixtures ↗</button></article><article class="league-page__panel"><span class="league-page__panel-label">RESULTS</span><div class="league-page__panel-icon">✓</div><h3>Recent results</h3><p>Review the latest completed matches and scores.</p><button class="league-page__panel-link" type="button" data-jump="matches">View results ↗</button></article><article class="league-page__panel"><span class="league-page__panel-label">STANDINGS</span><div class="league-page__panel-icon">#</div><h3>League table</h3><p>Track the teams competing for the top positions.</p><button class="league-page__panel-link" type="button" data-jump="table">Open table ↗</button></article></div></section><section class="league-page__content" data-content="matches"><div class="league-page__section-heading"><span>FIXTURES</span><h2>League Matches</h2><p>Live matches, upcoming fixtures and the latest completed results.</p></div><div class="league-page__fixture-section league-page__fixture-section--live"><div class="league-page__fixture-heading"><h3>Live Now</h3><span data-count="live">Loading…</span></div><div class="league-page__fixture-grid" data-fixtures="live"></div></div><div class="league-page__fixture-section"><div class="league-page__fixture-heading"><h3>Upcoming</h3><span data-count="upcoming">Loading…</span></div><div class="league-page__fixture-grid" data-fixtures="upcoming"></div></div><div class="league-page__fixture-section"><div class="league-page__fixture-heading"><h3>Recent Results</h3><span data-count="recent">Loading…</span></div><div class="league-page__fixture-grid" data-fixtures="recent"></div></div></section><section class="league-page__content" data-content="table"><div class="league-page__section-heading"><span>STANDINGS</span><h2>League Table</h2><p>Current positions, points and recent form.</p></div><div class="league-page__table-header"><span>LIVE STANDINGS</span><strong data-count="table">Loading…</strong></div><div data-standings></div></section><section class="league-page__content" data-content="teams"><div class="league-page__section-heading"><span>COMPETITION SQUAD</span><h2>League Teams</h2><p>All teams participating in this competition and season.</p></div><div class="league-page__table-header"><span>REGISTERED TEAMS</span><strong data-count="teams">Loading…</strong></div><div data-teams></div></section></section>`;
    app.querySelector(".league-page__back").addEventListener("click", () => { sessionStorage.removeItem(SELECTED_LEAGUE_KEY); window.location.hash = "#leagues"; });
    app.querySelector(".league-page__favorite").addEventListener("click", (event) => { const favorites = getFavorites(), id = competition.league.id, next = favorites.includes(id) ? favorites.filter((item) => item !== id) : [...favorites, id]; localStorage.setItem(FAVORITES_KEY, JSON.stringify(next)); event.currentTarget.classList.toggle("is-favorite", next.includes(id)); event.currentTarget.setAttribute("aria-pressed", String(next.includes(id))); });
    const activatePanel = (panel) => { app.querySelectorAll(".league-page__tab").forEach((tab) => tab.classList.toggle("is-active", tab.dataset.panel === panel)); app.querySelectorAll(".league-page__content").forEach((content) => content.classList.toggle("is-active", content.dataset.content === panel)); };
    app.querySelectorAll(".league-page__tab").forEach((tab) => tab.addEventListener("click", () => activatePanel(tab.dataset.panel)));
    app.querySelectorAll("[data-jump]").forEach((button) => button.addEventListener("click", () => activatePanel(button.dataset.jump)));
    loadLeagueMatches(app, competition); loadLeagueTable(app, competition); loadLeagueTeams(app, competition);
}

export default createLeaguePage;
